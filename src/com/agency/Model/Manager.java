package com.agency.Model;

public class Manager extends User{
    public Manager(String fullName, String uName, String pass, String userType) {
        super(fullName, uName, pass, userType);
    }
}
